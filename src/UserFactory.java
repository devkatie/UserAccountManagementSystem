
public class UserFactory {
	/*
	 * private static final int MAX = 1500; private NameWarehouse nw; private
	 * AccountBag ab;
	 * 
	 * public UserFactory(NameWarehouse nm, AccountBag ab) { this.nw = nw; this.ab =
	 * ab; loadGirls(); loadBoys(); }
	 * 
	 * public void loadGirls() { String[] firstNames = nw.getGirlsNames(); String[]
	 * lastNames = nw.getLastNames(); int count = 0; while (count < MAX) { User user
	 * = new User(firstNames[(int) (Math.random() * 500)], lastNames[(int)
	 * (Math.random() * 14000)]); user.setUsername(Username.generateUsername(user));
	 * user.setPassword(Password.generatePassword());
	 * user.setGpa(GPA.generateGPA()); user.setGender("F"); ab.addUser(user); } }
	 * 
	 * public void loadBoys() { String[] firstNames = nw.getBoysNames(); String[]
	 * lastNames = nw.getLastNames(); int count = 0; while (count < MAX) { User user
	 * = new User(firstNames[(int) (Math.random() * 500)], lastNames[(int)
	 * (Math.random() * 14000)]); user.setUsername(Username.generateUsername(user));
	 * user.setPassword(Password.generatePassword());
	 * user.setGpa(GPA.generateGPA()); user.setGender("M"); ab.addUser(user); } }
	 */
}
